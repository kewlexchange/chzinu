import React from 'react';
import { StyledHomeSection, StyledSection } from '../styledComponents/Containers';

const HomeSection = () => {
    return (
        <StyledHomeSection>
            HOME
        </StyledHomeSection>
    );
}

export default HomeSection;
