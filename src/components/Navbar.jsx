import React from 'react';
import { StyledNavBar } from './styledComponents/Containers';

const Navbar = () => {
    return (
        <StyledNavBar>
            
        </StyledNavBar>
    );
}

export default Navbar;
